BinRec Environment Variables
----------------------------

The BinRec toolkit requires several environment variables to work properly.
These are defined in the ``.env`` file and are loaded by the Python API
with the ``binrec.env`` module. Importing this module loads the
``.env`` file automatically.

.. automodule:: binrec.env
    :members:
