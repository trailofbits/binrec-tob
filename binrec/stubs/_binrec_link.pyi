def link(
    binary_filename: str,
    recovered_filename: str,
    runtime_library: str,
    linker_script: str,
    destination: str,
    dependencies_filename: str = None,
    harden: bool = False,
) -> None: ...
